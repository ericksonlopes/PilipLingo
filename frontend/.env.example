# Base da API. Em dev pode ficar vazio: o proxy do Vite encaminha /api -> backend.
VITE_API_URL=/api/v1

# Alvo do proxy de dev (usado apenas pelo vite.config.ts).
VITE_PROXY_TARGET=http://localhost:8000
